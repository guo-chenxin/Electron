// 导出各个模块的函数
import { deepClone, generateDefaultMenuItem, generateDefaultProjectForm, formatMenuItemOrders, validateRoutePath } from './commonUtils'

// 保持向后兼容性
export {
  deepClone,
  generateDefaultMenuItem,
  generateDefaultProjectForm,
  validateRoutePath,
  formatMenuItemOrders
}
